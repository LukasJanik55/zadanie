package book;

public class DamagedBook extends PhysicalBook {

    public DamagedBook(String title, String author, int year, String isbn, int value, int numberOfPages) {
        super(title, author, year, isbn, value, numberOfPages);
    }

}
