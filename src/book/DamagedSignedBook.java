package book;

public class DamagedSignedBook extends SignedBook {

    public DamagedSignedBook(String title, String author, int year, String isbn, int value, int numberOfPages) {
        super(title, author, year, isbn, value, numberOfPages);
    }

}
